-- Retired: the fresh rental-property system manages its own buildings.
-- Kept as a harmless placeholder so older builds cannot delete the new properties.
print("Legacy generated-house cleanup disabled")
